<?xml version="1.0" encoding="utf-8"?>
<templates>
  <template type="email" title="andy_inactivemembers" version_id="14" version_string="1.4"><![CDATA[<mail:subject>
	{{ phrase('inactivemembers_subject') }} ({{ phrase('inactivemembers_from') }} {$xf.options.boardTitle})
</mail:subject>

<xf:if is="$xf.options.inactiveMembersImageUrl != ''">
	<img src="{$xf.options.inactiveMembersImageUrl}" alt="image" />
</xf:if>

<br />

{{ phrase('inactivemembers_message', {
	'username': $username,
	'board_title': $xf.options.boardTitle
}) }}

<br />

<xf:foreach loop="$threads" value="$thread">
	<br />
	<div><a href="{{ link('canonical:threads/', $thread|censor) }}">{$thread.title|censor}</a></div>
	<div style="color:#8c8c8c;font-size:80%;">{$thread.forumTitle}</div>
</xf:foreach>

<br />
<a href="{{ link('canonical:account/preferences') }}">{{ phrase('inactivemembers_unsubscribe_link') }}</a>]]></template>
</templates>
